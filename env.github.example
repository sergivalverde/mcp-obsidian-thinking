# GitHub Mode Configuration Example
# Copy this file to .env and fill in your values

# Set mode to github
OBSIDIAN_MODE=github

# Path to your local vault (must be a git repository)
VAULT_PATH=/Users/username/Documents/ObsidianVault

# GitHub repository URL (SSH or HTTPS)
# SSH format (recommended if you have SSH keys set up):
GITHUB_REPO=git@github.com:username/my-vault.git

# HTTPS format (requires GITHUB_TOKEN):
# GITHUB_REPO=https://github.com/username/my-vault.git

# Optional: GitHub Personal Access Token (only needed for HTTPS repos)
# Create one at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

